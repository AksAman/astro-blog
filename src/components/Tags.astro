---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class="flex gap-2 mb-10 flex-wrap">
  {
    tags.map((tag) => {
      return (
        <a
          class="bg-gray-200 text-blue-600 rounded-md  text-xs px-2 py-1 h-8 flex items-center "
          href={`/tags/${tag}`}
        >
          {tag}
        </a>
      );
    })
  }
</div>
